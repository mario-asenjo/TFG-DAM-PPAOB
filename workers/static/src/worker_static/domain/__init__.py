"""Domain contracts for the static-analysis worker.

This package centralizes immutable value objects and canonical constant values
used across the worker lifecycle (job claim, analysis execution, and result
serialization). It does not perform I/O or orchestration on its own.
"""
