"""Static worker configuration package.

This package centralizes bootstrap-time configuration for the static analysis
worker. Configuration values are read from environment variables and exposed as
typed, immutable settings objects consumed by worker services.
"""
